{
  "name": "greenai",
  "version": "0.3.0",
  "private": true,
  "scripts": {
    "verify:openspec-docs": "node --test scripts/parity-docs-anchor.test.mjs",
    "verify:admin": "npm run build --prefix admin",
    "verify:backend": "npm run build --prefix backend && npm run test --prefix backend",
    "compose:up": "docker compose up -d",
    "compose:down": "docker compose down",
    "compose:logs": "docker compose logs -f",
    "compose:ps": "docker compose ps",
    "compose:db:up": "docker compose up -d db",
    "compose:mock:up": "docker compose up -d db_mock",
    "dev:uniapp:h5": "npm run dev:h5 --prefix uniapp",
    "icons:render": "node scripts/render-svg-icons.mjs",
    "ship": "node scripts/ship.mjs"
  },
  "devDependencies": {
    "sharp": "^0.33.5"
  }
}
